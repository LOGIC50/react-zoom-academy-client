import React from "react";
import { Container } from "react-bootstrap";

const Purchase = () => {
  return (
    <Container className="extra-route">
      <h1>Purchase isn't available right now...</h1>
    </Container>
  );
};

export default Purchase;
